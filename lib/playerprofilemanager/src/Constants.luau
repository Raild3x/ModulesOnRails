--!strict
-- Authors: Logan Hunt (Raildex)
-- Constants for PlayerProfileManager

--// Profile Loading Constants //--
local RETRY_PROFILE_LOAD_DELAY = 3
local RETRY_PROFILE_LOAD_ATTEMPTS = 5
local VERSION_KEY = "__VERSION"
local DEFAULT_DATA_STORE_KEY = "PPM_PlayerData"

--// Wipe Profile Constants //--
local WIPE_LOCK_TTL_SECONDS = 120
local WIPE_LOCK_MAP_NAME = "PPM_WipeLocks"
local WIPE_RETRY_ATTEMPTS = 3
local WIPE_RETRY_DELAY = 2
local WIPE_RESET_KICK_MESSAGE = "Your player data is being reset. Please rejoin in a moment."
local WIPE_MESSAGE_KIND = "ProfileWipeReset"

--// Save Priority Constants //--
local SAVE_PRIORITY = {
	HIGHEST = 1000,
	HIGH = 500,
	NORMAL = 0,
	LOW = -500,
	LOWEST = -1000,
}

return {
	RETRY_PROFILE_LOAD_DELAY = RETRY_PROFILE_LOAD_DELAY,
	RETRY_PROFILE_LOAD_ATTEMPTS = RETRY_PROFILE_LOAD_ATTEMPTS,
	VERSION_KEY = VERSION_KEY,
	DEFAULT_DATA_STORE_KEY = DEFAULT_DATA_STORE_KEY,
	WIPE_LOCK_TTL_SECONDS = WIPE_LOCK_TTL_SECONDS,
	WIPE_LOCK_MAP_NAME = WIPE_LOCK_MAP_NAME,
	WIPE_RETRY_ATTEMPTS = WIPE_RETRY_ATTEMPTS,
	WIPE_RETRY_DELAY = WIPE_RETRY_DELAY,
	WIPE_RESET_KICK_MESSAGE = WIPE_RESET_KICK_MESSAGE,
	WIPE_MESSAGE_KIND = WIPE_MESSAGE_KIND,
	SAVE_PRIORITY = SAVE_PRIORITY,
}
